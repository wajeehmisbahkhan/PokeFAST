package poke.fast.ui;


//This interface is only to pass in a different click event for each object. The interface is the same but implementation can be different 
public interface ClickListener {
	public void onClick();
}
